// examples/demo.ltl — runnable demo for ltl_procfs_walk

import std::io
use ltl_procfs_walk as lib

fn main() {
    io::println("ltl_procfs_walk demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}