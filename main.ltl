// ltl-procfs-walk/main.ltl
//
// Walker over /proc with per-pid stats collector.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-procfs-walk v0.1.0")
    io::println("Walker over /proc with per-pid stats collector.")
    0
}
